#!/usr/bin/env bash

set -euo pipefail

source "$DOTFILES_PATH/scripts/core/_main.sh"

##? Purge the branch that does not exist in the remote branch
#?? 1.0.0
##?
##? Usage:
##?    git-prune

docs::eval "$@"

git remote prune origin
