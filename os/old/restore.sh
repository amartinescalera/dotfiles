#!/usr/bin/env bash
mv "$HOME"/.aliases.bk "$HOME"/.aliases
mv "$HOME"/.bashrc.bk "$HOME"/.bashrc
mv "$HOME"/.gitconfig.bk "$HOME"/.gitconfig
mv "$HOME"/.gitignore_global.bk "$HOME"/.gitignore_global
mv "$HOME"/.profile.bk "$HOME"/.profile
mv "$HOME"/.zshrc.bk "$HOME"/.zshrc
